@model WebApplicationMvc.ViewModels.Maestro.MaestroCreateEditViewModel

@* Se muestra el uso de tag-helpers, que son etiquetas de ayuda de asp net *@
@* Permiten generar codigo de formulario en base al modelo que se definio para poder crear un registro *@

@* public int? Entero { get; set; } *@
@* public float? Flotante { get; set; } *@
@* public PruebaEnum? Enum { get; set; } *@
@* public string? Cadena { get; set; } *@
@* public DateTime? FechaHora { get; set; } *@
@* public DateTime? Fecha { get; set; } *@
@* public DateTime? Hora { get; set; } *@
@* public decimal? Decimal { get; set; } *@

<div class="row">
    <div class="col-3"></div>
    <div class="col-6">
        
        @* this not worrks when create, check *@
        
        @* <hr> *@
        @* *@
        @* <abp-dynamic-form abp-model="@Model"> *@
        @*     <div> *@
        @*         Some content.... *@
        @*     </div> *@
        @*     <div class="input-area"> *@
        @*         <abp-form-content /> *@
        @*     </div> *@
        @*     <div> *@
        @*         Some more content.... *@
        @*     </div> *@
        @* </abp-dynamic-form> *@
        @* *@
        @* <hr> *@
        
        <form asp-controller="Maestro" asp-action="Create">

            @* TODO: use test with form content for render all inputs, without send button *@

            <abp-input asp-for="Entero"></abp-input>
            <abp-input asp-for="Flotante"></abp-input>
            <abp-input asp-for="Cadena"></abp-input>
            <abp-select asp-for="Enum"></abp-select>
            <abp-input asp-for="FechaHora"></abp-input>
            <abp-input asp-for="Fecha"></abp-input>
            <abp-input asp-for="Hora"></abp-input>
            <abp-input asp-for="Decimal"></abp-input>
            
            <div class="row">
                <div class="col-12">
                    <button type="submit" class="btn btn-primary btn-block">Guardar</button>
                </div>
            </div>

        </form>
    </div>
    <div class="col-3"></div>
</div>

@section Scripts
{
    @* @await Html.PartialAsync("_ValidationScriptsPartial") *@
    <partial name="_ValidationScriptsPartial"/>
}